spring:
  profiles:
    active:
    - local
  application:
    name: eureka-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL}

---
#Configuration settings that used when the local profile is active
spring:
  profiles: local
  cloud:    config:      uri:      - http://localhost:8888